services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: legalyst
      POSTGRES_USER: legalyst
      POSTGRES_PASSWORD: legalyst
    ports:
      - "55432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
